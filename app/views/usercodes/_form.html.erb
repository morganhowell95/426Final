<%= form_for(@usercode) do |f| %>
  <% if @usercode.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@usercode.errors.count, "error") %> prohibited this usercode from being saved:</h2>

      <ul>
      <% @usercode.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :code %><br>
    <%= f.text_area :code %>
  </div>
  <div class="field">
    <%= f.label :user_id %><br>
    <%= f.number_field :user_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
